source ../scaling_settings.sh
 
echo "MAX CORES :$max_cores" 

for ((i = 1; i <= max_cores; i *= 2)); do
    echo "------------ Current thread count: $i ------------------"
    taskset -c $cores ./lavaMD -cores $i -boxes1d 30
done
